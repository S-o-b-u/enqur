{
  "version": 2,
  "builds": [
    {
      "src": "package.json",
      "use": "@vercel/next"
    }
  ],
  "env": {
    "NEXT_PUBLIC_APP_URL": "${VERCEL_URL}"
  },
  "buildCommand": "npm run build",
  "installCommand": "npm install"
}